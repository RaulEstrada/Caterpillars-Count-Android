<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.jforce.caterpillarscount.RegistrationActivity">

    <TextView
        android:id="@+id/registration_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textSize="20sp"
        android:textColor="#355D3D"
        android:text="Register for Caterpillars Count!"/>

    <EditText
        android:id="@+id/registration_fname"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:inputType="text"
        android:layout_below="@id/registration_title"
        android:hint="First name"/>
    <EditText
        android:id="@+id/registration_lname"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:inputType="text"
        android:layout_below="@id/registration_fname"
        android:hint="Last name"/>


    <EditText
        android:id="@+id/registration_emailfield"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:inputType="textEmailAddress"
        android:layout_below="@id/registration_lname"
        android:hint="Email address"/>

    <EditText
        android:id="@+id/registration_passwordfield"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:inputType="textPassword"
        android:layout_below="@id/registration_emailfield"
        android:hint="Password"/>

    <EditText
        android:id="@+id/registration_passwordconfirm"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:inputType="textPassword"
        android:layout_below="@id/registration_passwordfield"
        android:hint="Confirm password"/>


    <Button
        android:id="@+id/button_registration_register"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:textColor="#FFFFFF"
        android:background="#355D3D"
        android:text="Register"
        android:onClick="register"/>

</RelativeLayout>
